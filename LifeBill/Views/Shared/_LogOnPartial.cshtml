@using LifeBill.Models.Util;

@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@PubUtil.GetCookie(User.Identity.Name, Keys.NAME)</strong>!
    [ @Html.ActionLink("退出", "LogOff", "Home") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "Login", "Home") ]
}
